# Optional: default bounding box half-size in degrees when the frontend omits radius (default 0.5)
RADIUS_DEG=0.5

# Optional: seconds between OpenSky polls (default 10)
POLL_INTERVAL=10

# Optional: OpenSky OAuth2 credentials for authenticated access
# Leave blank for anonymous mode
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=
